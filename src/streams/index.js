const ReadStream = require('./readable.js')
const WritableStream = require('./writable.js')
const MyTransform = require('./transform.js')

module.exports = {
    ReadStream,
    WritableStream,
    MyTransform
}